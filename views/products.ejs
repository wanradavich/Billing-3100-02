<body>
    <div id="profile-body">
        <div>
            <h1><%= title %></h1>
            <ul>
                <% products.forEach(product => { %>
                    <li><%= product.productName %></li>
                <% }) %>
            </ul>
        </div>
    </div>
</body>
